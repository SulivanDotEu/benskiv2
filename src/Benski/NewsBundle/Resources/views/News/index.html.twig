{% extends 'BenskiNewsBundle:News:layout.html.twig' %}


{% block header %}
<h1>News list</h1>
{% endblock %}


{% block breadcrumb %}
{{ parent() }}
<li class="active">list</li>
{% endblock %}

{% block container_header_right %}
<a class="btn btn-primary" href="{{ path('news_new') }}">
   <i class="icon-plus icon-white"></i> 
   Create a new entry
</a>
{% endblock %}


{% block body -%}
    
    
    
    
    
    
    {% block menuFactory %}

    {% endblock %}
    
            
            
            
            
            
    
    
<h1>News list</h1>

<table class="table table-striped table-condensed">
   <thead>
      <tr>
        <th>Id</th>
        <th>Titre</th>
        <th>Contenu</th>
        <th>Resumé</th>
        <th>Auteur</th>
        <th>Tags</th>
      </tr>
   </thead>
   <tbody>
        {% for entity in entities %}
         <tr>
            <td><a href="{{ path('news_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
            <td><a href="{{ path('news_show', { 'id': entity.id }) }}">{{ entity.titre }}</a></td>
            <td>{{ entity.contenu }}</td>
            <td>{{ entity.resume }}</td>
            <td>{{ entity.auteur }}</td>
            <td>{{ entity.tag }}</td>
            <td>
               <div class="btn-group">
                  <a class="btn" href="{{ path('news_show', { 'id': entity.id }) }} ">
                     <i class="icon-eye-open"></i> Show</a>
                  <a class="btn" href="{{ path('news_edit', { 'id': entity.id }) }}">
                     <i class="icon-pencil"></i> Edit</a>
               </div>
            </td>
         </tr>
        {% endfor %}
      </tbody>
   </table>

   <ul>
      <li>
         <a href="{{ path('news_new') }}">
            Create a new entry
         </a>
      </li>
   </ul>
    {% endblock %}
