{% extends 'BenskiReservationBundle:AppartementReserve:layout.html.twig' %}

{% block body -%}
    <h1>AppartementReserve list</h1>

    <table class="table table-striped">
        <thead>
            <tr>
                <th>Id</th>
                <th>Sejour Reserve</th>
                <th>Numero</th>
                <th>Nombrepersonnes</th>
                <th>Prix</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('appartementreserve_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{{ entity.sejourReserve }}</td>
                <td>{{ entity.numero }}</td>
                <td>{{ entity.nombrePersonnes }}</td>
                <td>{{ macro.int2prix(entity.prix) }}€</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('appartementreserve_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('appartementreserve_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('appartementreserve_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
