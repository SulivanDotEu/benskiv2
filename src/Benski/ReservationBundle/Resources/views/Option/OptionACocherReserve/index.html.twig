{% extends "BenskiReservationBundle:Option:OptionACocherReserve/layout.html.twig" %}

{% block body -%}
    <h1>Option\OptionACocherReserve list</h1>

    <table class="table table-striped">
        <thead>
            <tr>
                <th>Id</th>
                <th>Prix</th>
                <th>Coche</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('option_optionacocherreserve_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{{ entity.prix }}</td>
                <td>{{ entity.coche }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('option_optionacocherreserve_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('option_optionacocherreserve_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('option_optionacocherreserve_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
