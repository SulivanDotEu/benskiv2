{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Option\OptionACocherReserve</h1>

    <table class="record_properties">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ entity.id }}</td>
            </tr>
            <tr>
                <th>Prix</th>
                <td>{{ entity.prix }}</td>
            </tr>
            <tr>
                <th>Coche</th>
                <td>{{ entity.coche }}</td>
            </tr>
        </tbody>
    </table>

        <ul class="record_actions">
    <li>
        <a href="{{ path('option_optionacocherreserve') }}">
            Back to the list
        </a>
    </li>
    <li>
        <a href="{{ path('option_optionacocherreserve_edit', { 'id': entity.id }) }}">
            Edit
        </a>
    </li>
    <li>{{ form(delete_form) }}</li>
</ul>
{% endblock %}
