{% extends "BenskiReservationBundle:Option:OptionChoixMultipleReserve/layout.html.twig" %}

{% block breadcrumb %}
{{ parent() }}
<li class="active">liste
</li> 
{% endblock %}

{% block body -%}
    <h1>Option\OptionChoixMultipleReserve list</h1>

    <table class="table table-striped">
        <thead>
            <tr>
                <th>Id</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('option_optionchoixmultiplereserve_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('option_optionchoixmultiplereserve_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('option_optionchoixmultiplereserve_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('option_optionchoixmultiplereserve_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
