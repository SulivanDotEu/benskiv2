{% extends "BenskiReservationBundle:Option:OptionReserve/layout.html.twig" %}



{% block breadcrumb %}
{{ parent() }}
<li class="active">list
</li> 
{% endblock %}

{% block container_header_right %}
<a class="btn btn-default" href="{{ path('option_optionchoixmultiplereserve') }}">
   <i class="icon-list "></i> 
   list of OptionChoixMultipleReserve
</a>
<a class="btn btn-default" href="{{ path('option_optionacocherreserve') }}">
   <i class="icon-list "></i> 
   list of OptionACocherReserve
</a>
{% endblock %}

{% block body -%}
<h1>Option\OptionReserve list</h1>

<table class="table table-striped">
   <thead>
      <tr>
         <th>Id</th>
         <th colspan="2">Option</th>
         <th>Prix</th>
         <th>Actions</th>
      </tr>
   </thead>
   <tbody>
        {% for entity in entities %}
      <tr>
         <td><a href="{{ path('option_optionreserve_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
         <td>{{ entity.option.id }}</td>
         <td>{{ entity.option.nom }}</td>
         <td>{{ macro.int2prix(entity.prix) }}€</td>
         <td>
            <ul>
               <li>
                  <a href="{{ path('option_optionreserve_show', { 'id': entity.id }) }}">show</a>
               </li>
               <li>
                  <a href="{{ path('option_optionreserve_edit', { 'id': entity.id }) }}">edit</a>
               </li>
            </ul>
         </td>
      </tr>
        {% endfor %}
   </tbody>
</table>

    {% endblock %}
