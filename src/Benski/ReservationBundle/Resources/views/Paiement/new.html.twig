{% extends 'BenskiReservationBundle:Paiement:layout.html.twig' %}

{% block body -%}
    <h1>Paiement creation</h1>

    {{ form(form) }}

        <ul class="record_actions">
    <li>
        <a href="{{ path('admin_paiement') }}">
            Back to the list
        </a>
    </li>
</ul>
{% endblock %}
