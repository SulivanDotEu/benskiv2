{% extends '::base.html.twig' %}

{% block body -%}
    <h1>ReservationSejour list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Id</th>
                <th>Responsable</th>
                <th>Total</th>
                <th>Statut</th>
                <th>Datecreation</th>
                <th>Datemodification</th>
                <th>Reductions</th>
                <th>Paiements</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('admin_reservation-sejour_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{{ entity.responsable }}</td>
                <td>{{ entity.total }}</td>
                <td>{{ entity.statut }}</td>
                <td>{% if entity.dateCreation %}{{ entity.dateCreation|date('Y-m-d H:i:s') }}{% endif %}</td>
                <td>{% if entity.dateModification %}{{ entity.dateModification|date('Y-m-d H:i:s') }}{% endif %}</td>
                <td>{{ entity.reductions }}</td>
                <td>{{ entity.paiements }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('admin_reservation-sejour_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('admin_reservation-sejour_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('admin_reservation-sejour_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
